let hlEnum = ["Whiskey", "Tequila", "Vodka", "Gin", "Brandy", "Rum"];
let naEnum = ['h'];
let bEnum = ['h'];
let gEnum = ["Olive", "Orange Peel", "Mint", "Lime"];
let oEnum = ['h'];

const ingredients = {
    hlEnum,
    gEnum,
    naEnum,
    bEnum,
    oEnum
}
module.exports = {
    ingredients
}