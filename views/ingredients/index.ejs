<%- include('../partials/header') %>
<h3>Ingredients</h3>
<div>
    <% allIngr.forEach((i) => { %> 
    <ul>
        <li> <%= `${i.ingrType} -- ${i.name}` %> </li>
    </ul>
    <% }); %>
</div>
<a href="../ingredients/new">Add new ingredient</a>
<%- include('../partials/footer') %>