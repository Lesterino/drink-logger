<%- include('../partials/header') %>
<h3>Ingredients</h3>
<div>
    <% allIng.forEach((i) => { %> 
    <ul>
        <li> <%= `${i.ingType} -- ${i.name}` %> <a href="/ingredients/<%= i._id %>">Edit</a> </li>
    </ul>
    <% }); %>
</div>
<a href="../ingredients/new">Add new ingredient</a>
<%- include('../partials/footer') %>